#ifndef PARSING_COMMAND_H
#define PARSING_COMMAND_H

#include <stdio.h>
#include <string.h>


int parsingCommand(char *input, char *command);
/* Program ini digunakan untuk memproses input pengguna dan 
mengekstrak command dari input tersebut.
*/

#endif /* PARSING_COMMAND_H */
